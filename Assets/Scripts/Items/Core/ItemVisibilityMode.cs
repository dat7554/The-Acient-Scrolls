namespace Items.Components.Weapons
{
    public enum ItemVisibilityMode
    {
        World,       // fully visible + shadow
        FirstPerson, // visible, no extra shadow
        ShadowOnly   // invisible, shadow only
    }
}